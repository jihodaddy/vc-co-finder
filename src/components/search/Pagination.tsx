'use client';

/**
 * Task 3 stub — numeric pagination with `paginationWindow()` lands next.
 */
type Props = { total: number };
// eslint-disable-next-line @typescript-eslint/no-unused-vars
export function Pagination({ total }: Props) {
  return null;
}
