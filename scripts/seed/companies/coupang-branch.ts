import type { SeedCompany } from '../types';

/**
 * Coupang branch placeholder.
 *
 * 쿠팡 관련 모든 alias는 coupang.ts에서 처리. 별도 법인 분리가 필요한 경우
 * (예: Coupang Inc. Delaware parent) 이 파일에 SeedCompany를 추가하면 됨.
 * Plan 02-05 Task 3a 옵션 (a) placeholder 경로.
 */
export {};

// eslint-disable-next-line @typescript-eslint/no-unused-vars
type _AliasOnlyMarker = SeedCompany;
