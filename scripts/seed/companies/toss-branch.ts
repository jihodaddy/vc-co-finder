import type { SeedCompany } from '../types';

/**
 * Toss branch placeholder.
 *
 * 비바리퍼블리카 법인과 토스 브랜드는 toss.ts aliases ('legal' + 'brand' +
 * 'english' × 2 + '토스뱅크' brand)로 통합 처리. SRCH-13 fixture 3종
 * (토스/토스뱅크/비바리퍼블리카) 전부 toss.ts에서 해결.
 * Plan 02-05 Task 3a 옵션 (a) placeholder 경로.
 */
export {};

// eslint-disable-next-line @typescript-eslint/no-unused-vars
type _AliasOnlyMarker = SeedCompany;
