import type { SeedCompany } from '../types';

/**
 * Kurly branch placeholder.
 *
 * 마켓컬리 브랜드와 컬리 법인은 kurly.ts에서 aliases 확장으로 통합 처리.
 * 이 파일은 tranche 1 지정 파일로 남겨두되 실제 SeedCompany는 export하지 않음.
 * Plan 02-05 Task 3a 옵션 (a) "alias 강화 후 placeholder" 경로.
 */
export {};

// eslint-disable-next-line @typescript-eslint/no-unused-vars
type _AliasOnlyMarker = SeedCompany;
