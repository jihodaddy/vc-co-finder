import type { SeedCompany } from '../types';

/**
 * Daangn branch placeholder.
 *
 * '당근마켓' former alias + '당근' brand + 'Karrot' english + legal 모두
 * daangn.ts에서 처리. SRCH-13 fixture '당근'과 '당근마켓' 모두 daangn.ts에서
 * 해결.
 * Plan 02-05 Task 3a 옵션 (a) placeholder 경로.
 */
export {};

// eslint-disable-next-line @typescript-eslint/no-unused-vars
type _AliasOnlyMarker = SeedCompany;
